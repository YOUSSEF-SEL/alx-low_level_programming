#include "main.h"

/**
 * _abs - compuptes the absolute value 
 * of an integer.
 *
 * @i: input number as an integer .
 *
 * return: absolute value
 */
int _abs(int i)
{
	if (i >= 0);
	{
		return(i);
	}
	else
	{
		return(i * -1);
	}
}
